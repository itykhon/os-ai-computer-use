[project]
name = "os_ai_os_macos"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.10"
classifiers = [
  "Programming Language :: Python :: 3",
]

dependencies = [
  "pyobjc>=11.0",
  "Pillow>=10",
  "os_ai_os>=0.1.0",
]

[tool.uv]
package = true

[project.entry-points."os_ai_os.drivers"]
darwin = "os_ai_os_macos.drivers:make_drivers"
