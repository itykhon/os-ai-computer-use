# Deprecated legacy file removed in new architecture.
# Intentionally left minimal to avoid import errors if any consumer still references it.
# Please migrate to platform drivers via os_ai_os.api.get_drivers().mouse and overlay.


